{
  "name": "vds-platform",
  "private": true,
  "workspaces": ["apps/*", "packages/*", "services/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/web\" \"npm run dev --workspace=services/api\"",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
